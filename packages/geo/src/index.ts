// @genealogy/geo — resolver implementations behind core's PlaceResolver
// interface. Network-touching; minimal in Step One (TRD §8.2).
//
// Implementations are added in the geo phase. This barrel grows as each lands.
export {};
